# Result - PyPI Readiness Dry Run

## Summary

PASS. ActCLI builds wheel and sdist artifacts, passes metadata checks,
installs into a fresh venv outside the repo, and runs all smoke commands
(`version`, `doctor`, `demo pricing-rnd`) from the installed console
script. All six expected demo artifacts are produced and pass content
and safety checks.

## Product Commit

- branch: `feature/evaluation-path/pypi-readiness-dry-run`
- commit: `117be0fc9f8fdcae3b1a4862226c2a56bf9c0109`

## Checks Run

- `python -m pytest tests/integration/test_demo_pricing_rnd.py -q` - PASS (2 passed)
- `python -m build` - PASS (wheel and sdist created)
- `python -m twine check dist/*` - PASS (both artifacts)
- fresh venv wheel install outside repo - PASS
- `actcli version` from installed wheel - PASS (`ActCLI 0.0.1`)
- `actcli doctor` from installed wheel - PASS (all env checks nominal)
- `actcli demo pricing-rnd --out "$VERIFY_DIR/pricing-rnd"` from installed
  wheel - PASS (3 participants, 2 rounds, 6 artifacts)
- artifact content/safety check - PASS (all assertions pass)

## Environment

- host: `Acer-HL`
- Python: 3.11.2
- venv: `$HOME/.venvs/actcli-python`
- wheel path tested: `dist/actcli-0.0.1-py3-none-any.whl`
- sdist path created: `dist/actcli-0.0.1.tar.gz`
- verify venv: `/tmp/actcli-verify-*/venv` (temp dir, destroyed on reboot)

## Behavior Notes

- `actcli version` prints `ActCLI 0.0.1` and exits 0.
- `actcli doctor` runs all environment checks; TTY and color show WARN in
  non-interactive mode as expected; ollama/API keys show appropriate INFO/WARN.
- `actcli demo pricing-rnd` runs two rounds with three local-demo EchoAdapter
  personas; synthesis and disagreement score computed correctly.
- All six output artifacts (`README.md`, `prompt.md`, `transcript.md`,
  `workpaper.md`, `audit.json`, `repro.sh`) created in the output directory.

## Packaging Caveats

- `python3-venv` is not installed on this host, causing `python -m venv` to
  fail for fresh venvs. The fresh-venv smoke test was completed by creating a
  `--without-pip` venv and bootstrapping pip into it from the shared dev venv.
  This is a host configuration issue, not a packaging defect.
- `build` and `twine` were added to `[project.optional-dependencies] dev`
  because the sprint brief requires them for packaging checks and they were
  not previously declared.
- The `repro.sh` generated by the installed wheel still references
  `python -m actcli` which assumes an editable install; this is acceptable for
  the dry-run scope but should be corrected before a real PyPI release.

## Pushback Or Blockers

None. All acceptance targets met.

## Validation Handoff

Ready for:

`validation/evaluation-path/pypi-readiness-dry-run`
